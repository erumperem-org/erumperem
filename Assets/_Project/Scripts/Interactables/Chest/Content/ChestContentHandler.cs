using System.Collections.Generic;
using UnityEngine;
public class ChestContentHandler : MonoBehaviour
{
    static List<IStorageable> SetChestContent(ChestInteractable chest, Dictionary<IStorageable, float> itenToSort, int maxItens)
    {
        List<IStorageable> result = new List<IStorageable>();

        return result;
    }
}